# Legacy tests

Tests kept for reference when restoring archived Python packages. They are **not**
collected by default (`norecursedirs` in `pyproject.toml`).

| File | Requires |
|------|----------|
| `test_dataset_adapters.py` | `dualexis.datasets` (external-dataset adapters; not in TSGG/JSS scope) |
