% Harness honesty tables — synthetic reference implementation; descriptive only.

\begin{table}[htbp]
  \centering
  \caption{Framework-validation disclosure summary (Stage~S1 synthetic harness).
    Descriptive audit descriptors only; not operational outcomes, readiness, or system quality.}
  \label{tab:harness-honesty}
  \small
  \begin{tabular}{@{}p{5.9cm}r@{}}
    \toprule
    Metric & Value \\
    \midrule
    Leakage score $L_S$ (higher = more coupling) & 0.300 \\
    Procedural independence $\pi_{\mathrm{proc}}$ & 1.000 \\
    Distributional independence $\pi_{\mathrm{dist}}$ & 0.698 \\
    Privacy fuzz pass rate ($N=10$ probes) & 1.000 \\
    Governance compliance $\gamma_{\mathrm{comp}}$ & 0.440 \\
    Governance decision traceability $\tau_{\mathrm{trace}}$ & 1.000 \\
    Mean path trust $\bar{T}_\pi$ (trust propagation) & 0.060 \\
    \bottomrule
  \end{tabular}
  \par\vspace{0.4em}
  {\footnotesize\emph{Statement:} rows are disjoint framework-validation descriptors on the reference
  exporter, not a composite score. $\pi_{\mathrm{proc}}{=}1$ is a necessary code-separation check, not
  distributional independence ($\pi_{\mathrm{dist}}{=}0.698$; GT stability ${\approx}0.5$ under MC
  perturbation). $\tau_{\mathrm{trace}}$ is governance audit-chain completeness on simulated exports;
  $\gamma_{\mathrm{comp}}$ is partial policy alignment ($0.440$). $\bar{T}_\pi$ is compositional
  feasibility under A6, not operator trust. Results validate harness disclosure, not field utility.}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{Reference orchestration path \textbf{harness honesty labels} vs.\ procedural ground truth by
    scenario ($N=30$ seeds). Pass/Partial/Fail document placeholder-pipeline divergence for benchmark
    transparency, not TSGG schema conformance or detection capability. Multiseed B1--B5 harness
    diagnostics: online supplementary material (Appendix~\ref{app:online-material}).}
  \label{tab:harness-b5-by-scenario}
  \small
  \begin{tabular}{@{}ll@{}}
    \toprule
    Scenario & Outcome \\
    \midrule
    normal\_flow & Pass \\
    exit\_blockage & Partial \\
    multimodal\_conflict & Fail \\
    evacuation\_recommendation & Fail \\
    crowd\_acceleration & Fail \\
    audio\_stress\_signal & Fail \\
    \bottomrule
  \end{tabular}
\end{table}
